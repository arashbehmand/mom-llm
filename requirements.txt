fastapi==0.136.1
# Cap starlette: fastapi 0.136.1 only requires starlette>=0.46.0 (no upper bound),
# but starlette 1.0.0 removed the on_startup/on_shutdown Router kwargs that fastapi
# still passes internally, crashing app/router init on a clean build.
starlette>=0.46.0,<1.0.0
langfuse==4.5.1
litellm==1.88.0
python-dotenv==1.2.2
PyYAML==6.0.3
uvicorn==0.46.0
watchfiles==1.1.1
# Pin the major: an unpinned float to a newer redis-py changed async pubsub
# idle-read behavior and silently broke the reporting listener in prod.
redis>=5.0.0,<7.0.0

# Testing dependencies
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
httpx==0.28.1
respx==0.23.1

# Code quality tools
black
ruff
pre-commit
